/* eslint-disable camelcase */
export const generateBid = (interestGroup, auctionSignals, perBuyerSignals, trustedBiddingSignals, browserSignals) => ({
	ad: {
		good: 'stuff',
	},
	bid: 1,
	render: 'http://url.com',
});
